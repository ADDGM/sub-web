name: Build

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run linter
        run: yarn lint:check

      - name: Build
        run: yarn build:subpath

      - name: Write build metadata
        run: |
          BUILD_INFO_DIR="$(dirname "$(find dist -name index.html -print -quit)")"
          {
            echo "event=${GITHUB_EVENT_NAME}"
            echo "ref=${GITHUB_REF}"
            echo "ref_name=${GITHUB_REF_NAME}"
            echo "sha=$(git rev-parse HEAD)"
            echo "run_id=${GITHUB_RUN_ID}"
            echo "run_number=${GITHUB_RUN_NUMBER}"
          } > "${BUILD_INFO_DIR}/build-info.txt"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist-node${{ matrix.node-version }}
          path: dist/
          retention-days: 7

      # ==================== TG 通知 ====================
      - name: Telegram 通知
        if: always() # 确保无论成功还是失败都会发送通知
        continue-on-error: true
        uses: appleboy/telegram-action@v1.0.1
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            *GitHub Actions CI 构建报告*

            *工作流*: `${{ github.workflow }}`
            *状态*: ${{ job.status == 'success' && '✅ 构建成功' || '❌ 构建失败' }}
            *触发事件*: `${{ github.event_name }}`
            *提交*: [${{ github.sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})
            *分支*: `${{ github.ref_name }}`
            *触发人*: ${{ github.actor }}
